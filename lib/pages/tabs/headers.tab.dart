import 'package:flutter/material.dart';

class HeadersTab extends StatelessWidget {
  const HeadersTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Headers Tab'),
    );
  }
}
